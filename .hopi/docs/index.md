# HOPI Project Context

Project: CardGame

## Working Rules

- Repo docs are long-term memory.
- Ask one blocking question when product intent is unclear.
- Keep kanban tasks small and verifiable.

## Active Goal Context

- 2026-05-14: Goal `goal` / `439c8289-a3f4-4bad-9f75-45d60ed1b05d` — 新建教学关. Brainstorming selected a standalone data-driven tutorial mode rather than a hardcoded demo scene or mutation of normal routes. Current `dev` now has both gameplay deterministic seams needed for the foundation: tutorial Expedition fixed outcome plus opt-in `deterministicBattleSetup.deckOrder = preserve-json-order` for Battle deck startup, while ordinary Story / Expedition / default Battle starts still shuffle by default. The tutorial outer-mountain Expedition map/events/shop/worldSeed/deck/encounter content is also present. Milestone backstop was assessed on 2026-05-14 and not used because the remaining foundation gaps are still clear, necessary, and bounded rather than speculative: the durable tutorial script matrix doc and the tutorial WorldMap / Hub / Story catalog skeleton. Current focus is to finish the playable wiring for the tutorial. The tutorial MainMenu entry and Session/Reset logic have been dispatched. The next batch will unblock and promote the tutorial UI guidance (tutorial-hint-ui) and end-to-end smoke test (tutorial-e2e-smoke) to wrap up the tutorial implementation.
- 2026-05-10: Goal `d252bb99-35c4-460e-b1f3-b3d3fe05af80` — 调整为成熟的游戏架构. The read-only catalog, cross-resource validation, runtime catalog loading, save compatibility/document/restore seams, status/gongfa/config validation, runtime Realm/Artifact config seeding, restricted gongfa numeric expression evaluator (`3d7dda6`), injected-storage save snapshot reads (`e73a22b`), canonical realm/grade config validation (`b3a0906`), pure gongfa card filtering (`d557363`), injected save document transfer (`02e560f`), initial world-seed stash/deck/item validation (`0f94b78`), pure initial seed bootstrap (`0b106e0`), injected save-transfer readback verification (`962d146`), typed gongfa card-flow operations (`d0bb287`), typed `GainArmor` operations (`ad2cf52`), read-only `GameWorldState` projection (`8ba0c8b`), pure save-document migration pipeline (`1f4db5d`), pure save-document JSON codec (`67c11d5`), pure `GameWorldState` persistent-stash operations (`83b7d65`), typed `ImmediateAttack` adapter (`41aaa51`), injected persistent-stash writes (`65098e8`), gongfa action dispatch registry (`e56db05`), manual JSON save-document text transfer (`9617669`), pure gongfa condition evaluation (`566c98a`), content catalog validation-index split (`04972b9`), and a narrow `GameWorldState` persistent-stash write boundary (`5cf5611`) have landed.
- Current focus: block further mature-architecture generator promotion for a human milestone review. The architecture contract spine now has substantial accepted catalog/runtime-loading, save/world-state, and gongfa/stat-effect seams; remaining candidates are mostly priority choices, docs cleanup, or speculative deeper refactors that should be validated against real product/content direction before more generator work.
- 2026-05-10: Goal `05dce62b-91c1-4dd7-a8e9-f9346b1bc7a4` — 故事系统. The human-selected two-level map hierarchy now has its first Hub sub-map presentation slice landed in commit `e20c899`; current focus remains blocked on one concrete post-sub-map product choice before promoting more implementation work. Do not create speculative story-system generator tasks until the human chooses between real second-level content, unlock/world-state ownership, durable current-position/map-state behavior, Hub operations, Expedition/story-content integration, or content-rich secret-realm work.

## Recent story-system background

- Goal `05dce62b-91c1-4dd7-a8e9-f9346b1bc7a4` — 故事系统 was manually restarted on 2026-05-09 after the first vertical slice landed. The `MainMenu` story entry, writer-facing authoring docs, compact checked schema example, pure story-triggered combat contract, minimal `StoryScene` / `BattleScene` / `StoryScene` battle round-trip, data-driven Hub shell, Hub sublocation navigation, Story/Hub session persistence, second Hub-launched tea-house side-story graph, thin data-driven `WorldMapScene` launcher, `HubScene | ExpeditionScene -> WorldMapScene` return/resume loop, authoritative world-map destination payloads, one independent Qingyun sect-gate Hub destination, one direct Qingyun town tea-house Hub-location destination, route-keyed Expedition active-run persistence, first additional Expedition/秘境 world-map destination, and draggable spatial world-map marker presentation are now in place. Commits `3526750` / `df58c82` replaced the destination menu with data-driven marker coordinates, a pannable map surface, marker-click routing, drag threshold guards, and focused validation. The latest completed story-system continuation is the first two-level map hierarchy proof: first-level world-map nodes such as towns now open a similar draggable Hub sub-map with scattered second-level location markers, and clicking a second-level marker opens that location's existing action/options panel while route/session/action ownership remains unchanged. After commit `e20c899`, do not auto-bundle the next step; ask the human to choose one concrete continuation such as real second-level location content, unlock/world-state ownership, durable current-position/map-state behavior, Hub operations, Expedition/story-content integration, or content-rich secret realm work.
