---
phase: "01-first-playable-expedition"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01"]
files_modified:
  - "src/game/main.ts"
  - "src/game/scenes/Boot.ts"
  - "src/game/scenes/expedition/ExpeditionScene.ts"
  - "src/game/ui/expedition/PreparationPanel.ts"
  - "src/game/ui/expedition/RunHud.ts"
  - "src/game/ui/expedition/MapNodeView.ts"
  - "src/game/ui/expedition/NodePanel.ts"
autonomous: true
requirements: ["RUN-01", "MAP-01", "MAP-02", "MAP-03", "NODE-02"]
user_setup: []
must_haves:
  truths:
    - "Player can review the starter stash at the entrance before starting a run"
    - "Only connected reachable nodes are selectable on the expedition map"
    - "Event, shop, and extract nodes resolve inside the expedition scene without losing active run state"
  artifacts:
    - path: "src/game/scenes/expedition/ExpeditionScene.ts"
      provides: "Preparation flow, map rendering, and non-combat node orchestration"
      exports: ["ExpeditionScene"]
    - path: "src/game/ui/expedition/PreparationPanel.ts"
      provides: "Entrance stash review and run confirmation UI"
      exports: ["PreparationPanel"]
    - path: "src/game/ui/expedition/MapNodeView.ts"
      provides: "Node rendering with visibility and reachability states"
      exports: ["MapNodeView"]
    - path: "src/game/ui/expedition/NodePanel.ts"
      provides: "Event, shop, and extract node panels"
      contains: "extract"
  key_links:
    - from: "src/game/scenes/expedition/ExpeditionScene.ts"
      to: "src/game/state/ExpeditionState.ts"
      via: "stash review and active run mutation"
      pattern: "createRunSnapshot|activeRun|persistentStash"
    - from: "src/game/scenes/expedition/ExpeditionScene.ts"
      to: "src/game/ui/expedition/MapNodeView.ts"
      via: "node graph rendering"
      pattern: "MapNodeView|renderNode|isReachableNode"
    - from: "src/game/ui/expedition/NodePanel.ts"
      to: "public/data/mijing/prototype-events.json"
      via: "event and shop content lookup"
      pattern: "nodeId|rewards|offers"
---

<objective>
Create the expedition entry scene, fog-of-war map traversal, and non-combat node flow for Phase 01.

Purpose: Turn the new stash and run state into a playable exploration surface before combat handoff is connected.
Output: Expedition scene, entrance stash review panel, HUD, map node rendering, and in-scene event / shop / extract panels.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-playable-expedition/01-CONTEXT.md
@src/game/main.ts
@src/game/scenes/Boot.ts
@src/game/EventBus.ts
@src/game/state/ExpeditionState.ts
@src/game/services/RunPersistence.ts
@src/game/config/LayoutConfig.ts
@public/data/mijing/prototype-map.json
@public/data/mijing/prototype-events.json
@public/data/mijing/prototype-shop.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot into an expedition entry flow with stash review</name>
  <files>src/game/main.ts, src/game/scenes/Boot.ts, src/game/scenes/expedition/ExpeditionScene.ts, src/game/ui/expedition/PreparationPanel.ts, src/game/ui/expedition/RunHud.ts</files>
  <read_first>src/game/main.ts, src/game/scenes/Boot.ts, src/game/config/LayoutConfig.ts, src/game/EventBus.ts, src/game/state/ExpeditionState.ts, public/data/world/initial-state.json</read_first>
  <action>Create `ExpeditionScene` plus `PreparationPanel` and `RunHud` under `src/game/scenes/expedition` and `src/game/ui/expedition`. Register `ExpeditionScene` in `src/game/main.ts`, and change `Boot.ts` to start `ExpeditionScene` instead of `BattleScene`. In `ExpeditionScene.create`, load the persistent stash and active run through `ExpeditionState`. If no active run exists, show `PreparationPanel` with the starter deck, starter items, and current `spiritStones`, plus a single confirm action (for Phase 01 there is one starter stash, not a full deckbuilder). On confirmation, call `createRunSnapshot`, hide the preparation panel, and show `RunHud` with current node, carried deck count, carried item count, and `spiritStones`.</action>
  <verify>rg -n 'ExpeditionScene' src/game/main.ts src/game/scenes/Boot.ts src/game/scenes/expedition/ExpeditionScene.ts && rg -n 'PreparationPanel|createRunSnapshot|spiritStones|carriedDeck|carriedItems' src/game/scenes/expedition/ExpeditionScene.ts src/game/ui/expedition/PreparationPanel.ts src/game/ui/expedition/RunHud.ts</verify>
  <acceptance_criteria>
    - src/game/main.ts contains `ExpeditionScene`
    - src/game/scenes/Boot.ts contains `this.scene.start('ExpeditionScene')`
    - src/game/ui/expedition/PreparationPanel.ts contains `confirm` or `confirmLoadout`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `createRunSnapshot(` and `PreparationPanel`
  </acceptance_criteria>
  <done>The game boots into an expedition entry flow backed by persistent stash and run state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fog-of-war graph traversal and reachable-node interaction</name>
  <files>src/game/scenes/expedition/ExpeditionScene.ts, src/game/ui/expedition/MapNodeView.ts</files>
  <read_first>public/data/mijing/prototype-map.json, src/game/scenes/expedition/ExpeditionScene.ts, src/game/state/ExpeditionState.ts, public/data/docs/秘境.md</read_first>
  <action>Create `MapNodeView` to render icons and labels for `entrance`, `battle`, `event`, `shop`, `extract`, and `boss` nodes. In `ExpeditionScene`, compute visibility so cleared nodes are fully visible, directly connected next nodes are interactive, and deeper future nodes render as silhouettes only. Allow clicks only on node ids present in the current node's outgoing edge list. When the player enters a node, mark it visited in `activeRun.nodeStates`, update visibility, and route by type: `battle` and `boss` should record a `pendingEncounter` payload for the next plan, while `event`, `shop`, and `extract` stay in-scene.</action>
  <verify>rg -n 'silhouette|isReachableNode|getVisibleNodes|pendingEncounter|visited' src/game/scenes/expedition/ExpeditionScene.ts src/game/ui/expedition/MapNodeView.ts</verify>
  <acceptance_criteria>
    - src/game/ui/expedition/MapNodeView.ts contains `'entrance'`, `'battle'`, `'event'`, `'shop'`, `'extract'`, and `'boss'`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `isReachableNode(` and `getVisibleNodes(`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `pendingEncounter`
    - src/game/scenes/expedition/ExpeditionScene.ts mutates node visit state when a node is entered
  </acceptance_criteria>
  <done>Players can traverse only visible connected nodes with fog-of-war behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve event, shop, and extract nodes inside the expedition scene</name>
  <files>src/game/scenes/expedition/ExpeditionScene.ts, src/game/ui/expedition/NodePanel.ts, src/game/ui/expedition/RunHud.ts</files>
  <read_first>src/game/scenes/expedition/ExpeditionScene.ts, src/game/state/ExpeditionState.ts, src/game/ui/expedition/RunHud.ts, public/data/mijing/prototype-events.json, public/data/mijing/prototype-shop.json</read_first>
  <action>Create `NodePanel` to render three concrete non-combat flows. Event nodes must roll exactly one reward from the node's event pool and apply it to `activeRun.pendingLoot` or `spiritStones`. Shop nodes must list concrete offers from `prototype-shop.json`, block purchase when `spiritStones` is insufficient, and add bought cards or items into the carried run inventory. Extract nodes must present a confirmation action that sets `pendingResolution = 'extract'` and returns control to the expedition scene; do not mutate the permanent stash here because Plan 03 owns terminal resolution.</action>
  <verify>rg -n 'pendingLoot|pendingResolution|extract|purchase|spiritStones' src/game/scenes/expedition/ExpeditionScene.ts src/game/ui/expedition/NodePanel.ts src/game/ui/expedition/RunHud.ts && npm run build-nolog</verify>
  <acceptance_criteria>
    - src/game/ui/expedition/NodePanel.ts contains `event`, `shop`, and `extract`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `pendingLoot`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `pendingResolution = 'extract'` or an equivalent exact extract-state string
    - src/game/scenes/expedition/ExpeditionScene.ts contains a purchase guard using `spiritStones`
  </acceptance_criteria>
  <done>Non-combat nodes work in-scene and preserve run state for terminal resolution later.</done>
</task>

</tasks>

<verification>
- [ ] npm run build-nolog succeeds
- [ ] ExpeditionScene is the default boot target
- [ ] Reachable-node logic and non-combat node flows exist with the exact states named in this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The player can start a run, move through a fogged map, and resolve non-combat nodes without losing run state
</success_criteria>

<output>
After completion, create `.planning/phases/01-first-playable-expedition/01-02-SUMMARY.md`
</output>
