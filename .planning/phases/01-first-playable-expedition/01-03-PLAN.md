---
phase: "01-first-playable-expedition"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02"]
files_modified:
  - "public/data/encounters/mijing-boss.json"
  - "src/game/EventBus.ts"
  - "src/game/scenes/expedition/ExpeditionScene.ts"
  - "src/game/scenes/battle/BattleScene.ts"
  - "src/game/managers/battle/BattleStateChecker.ts"
  - "src/game/managers/battle/TurnManager.ts"
  - "src/game/services/RunResolution.ts"
autonomous: true
requirements: ["NODE-01", "OUT-01", "OUT-02", "OUT-03"]
user_setup: []
must_haves:
  truths:
    - "Battle and boss nodes use the active run deck and node encounter payload"
    - "Defeat wipes carried run assets while extract or boss clear banks them into persistent stash"
    - "After terminal resolution, the player can see kept or lost assets and start a fresh run from the entrance"
  artifacts:
    - path: "src/game/services/RunResolution.ts"
      provides: "Canonical run-outcome mutation rules"
      exports: ["resolveBattleVictory", "resolveBattleDefeat", "resolveBossClear", "resolveExtract"]
    - path: "src/game/scenes/battle/BattleScene.ts"
      provides: "Battle scene launch and completion hooks for expedition nodes"
      contains: "handleBattleEnd"
    - path: "src/game/managers/battle/BattleStateChecker.ts"
      provides: "Battle-end flow that returns to expedition resolution instead of hard restarting the scene"
      contains: "handleBattleEnd"
    - path: "src/game/scenes/expedition/ExpeditionScene.ts"
      provides: "Run result handling and post-run summary"
      contains: "boss-clear"
  key_links:
    - from: "src/game/scenes/expedition/ExpeditionScene.ts"
      to: "src/game/scenes/battle/BattleScene.ts"
      via: "scene launch payload for combat nodes"
      pattern: "this\.scene\.(launch|start)\('BattleScene'"
    - from: "src/game/scenes/battle/BattleScene.ts"
      to: "src/game/EventBus.ts"
      via: "battle completion event"
      pattern: "expedition-battle-complete"
    - from: "src/game/services/RunResolution.ts"
      to: "src/game/services/RunPersistence.ts"
      via: "terminal stash mutation"
      pattern: "savePersistentStash|clearActiveRun"
---

<objective>
Connect battle nodes, extract flow, and permanent stash mutation so Phase 01 becomes a full keep-or-lose expedition loop.

Purpose: Reuse the existing battle scene as the combat resolver and make its outcomes permanently matter to the player's stash.
Output: Battle launch payload integration, prototype boss encounter, canonical run-resolution service, and a post-run summary back at the entrance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-playable-expedition/01-CONTEXT.md
@src/game/EventBus.ts
@src/game/scenes/expedition/ExpeditionScene.ts
@src/game/scenes/battle/BattleScene.ts
@src/game/managers/battle/BattleTickManager.ts
@src/game/managers/battle/BattleStateChecker.ts
@src/game/managers/battle/TurnManager.ts
@src/game/state/ExpeditionState.ts
@src/game/services/RunPersistence.ts
@public/data/encounters/medium-enemy.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch battle and boss nodes from expedition state</name>
  <files>src/game/scenes/expedition/ExpeditionScene.ts, src/game/scenes/battle/BattleScene.ts, public/data/encounters/mijing-boss.json</files>
  <read_first>src/game/scenes/expedition/ExpeditionScene.ts, src/game/scenes/battle/BattleScene.ts, src/game/state/ExpeditionState.ts, public/data/encounters/medium-enemy.json</read_first>
  <action>Add a launch contract to `BattleScene` using `BattleLaunchPayload` from `src/game/types/expedition.ts`. Implement `init(data)` in `BattleScene` so battle nodes can override the default `starterDeck` and `currentEncounter` sources with `{ nodeId, encounterId, encounterType, runDeck }`, while preserving the current JSON fallback path when the scene is launched standalone. Add `public/data/encounters/mijing-boss.json` for the prototype boss node. In `ExpeditionScene`, when `pendingEncounter` points to a battle or boss node, pause or hide the expedition scene and start `BattleScene` with that payload.</action>
  <verify>rg -n 'init\(data|encounterId|encounterType|runDeck|nodeId' src/game/scenes/battle/BattleScene.ts src/game/scenes/expedition/ExpeditionScene.ts && rg -n 'mijing-boss|"units"' public/data/encounters/mijing-boss.json</verify>
  <acceptance_criteria>
    - src/game/scenes/battle/BattleScene.ts contains `init(data` and a scene-data read path
    - src/game/scenes/battle/BattleScene.ts no longer relies on `starterDeck` as the only loadout source
    - public/data/encounters/mijing-boss.json contains `"units"`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `this.scene.start('BattleScene'` or `this.scene.launch('BattleScene'`
  </acceptance_criteria>
  <done>Battle and boss nodes launch the existing combat scene using run-specific payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve battle, extract, and boss outcomes into permanent stash mutations</name>
  <files>src/game/services/RunResolution.ts, src/game/EventBus.ts, src/game/scenes/battle/BattleScene.ts, src/game/managers/battle/BattleStateChecker.ts, src/game/managers/battle/TurnManager.ts, src/game/scenes/expedition/ExpeditionScene.ts</files>
  <read_first>src/game/EventBus.ts, src/game/scenes/battle/BattleScene.ts, src/game/managers/battle/BattleTickManager.ts, src/game/managers/battle/BattleStateChecker.ts, src/game/managers/battle/TurnManager.ts, src/game/scenes/expedition/ExpeditionScene.ts, src/game/services/RunPersistence.ts, public/data/docs/秘境.md</read_first>
  <action>Create `src/game/services/RunResolution.ts` with `resolveBattleVictory`, `resolveBattleDefeat`, `resolveBossClear`, and `resolveExtract`. Implement `BattleScene.handleBattleEnd(victory)` because `BattleTickManager` already looks for it, and emit a shared EventBus message `expedition-battle-complete` carrying `RunResolutionSummary`. Replace the direct `this.scene.scene.restart()` callbacks in `BattleStateChecker.ts` with post-overlay callbacks that delegate into `BattleScene.handleBattleEnd(victory)`. Update `TurnManager.showVictory` and `showDefeat` so the acknowledgement CTA can return to the expedition flow instead of always restarting the battle scene. On defeat, remove all carried or looted cards, items, and `spiritStones` from the stash snapshot and clear the active run. On extract or boss clear, merge carried deck, carried items, pending loot, and `spiritStones` into the persistent stash and clear the active run.</action>
  <verify>rg -n 'resolveBattleVictory|resolveBattleDefeat|resolveBossClear|resolveExtract' src/game/services/RunResolution.ts && rg -n 'expedition-battle-complete|handleBattleEnd|outcome: .*(defeat|success)' src/game/services/RunResolution.ts src/game/scenes/battle/BattleScene.ts src/game/EventBus.ts && ! rg -n 'scene\.restart' src/game/managers/battle/BattleStateChecker.ts</verify>
  <acceptance_criteria>
    - src/game/services/RunResolution.ts contains `resolveBattleDefeat` and `resolveExtract`
    - src/game/services/RunResolution.ts contains explicit defeat and success outcome payloads
    - src/game/scenes/battle/BattleScene.ts contains `handleBattleEnd(`
    - src/game/managers/battle/BattleStateChecker.ts does not contain `scene.restart`
  </acceptance_criteria>
  <done>Defeat, extract, and boss clear all mutate the persistent stash according to the product rules.</done>
</task>

<task type="auto">
  <name>Task 3: Return to the entrance with a readable post-run summary</name>
  <files>src/game/scenes/expedition/ExpeditionScene.ts, src/game/ui/expedition/RunHud.ts, src/game/services/RunResolution.ts</files>
  <read_first>src/game/scenes/expedition/ExpeditionScene.ts, src/game/ui/expedition/RunHud.ts, src/game/services/RunResolution.ts, .planning/REQUIREMENTS.md</read_first>
  <action>After any terminal outcome, show a summary overlay or refreshed entrance panel listing the exact cards, items, and `spiritStones` kept or lost. The summary must distinguish `defeat`, `extract`, and `boss-clear`. Once acknowledged, reload the persistent stash from storage, reset the expedition scene to the entrance state, and allow the player to start a fresh run without manual localStorage cleanup or a full page refresh.</action>
  <verify>rg -n 'boss-clear|extract|defeat|lostCards|keptCards|startFreshRun|returnToEntrance' src/game/scenes/expedition/ExpeditionScene.ts src/game/ui/expedition/RunHud.ts && npm run build-nolog</verify>
  <acceptance_criteria>
    - src/game/scenes/expedition/ExpeditionScene.ts contains exact terminal labels or enums for `defeat`, `extract`, and `boss-clear`
    - src/game/scenes/expedition/ExpeditionScene.ts contains `startFreshRun(` or `returnToEntrance(`
    - src/game/ui/expedition/RunHud.ts contains summary fields for kept and lost inventory
    - `npm run build-nolog` exits 0
  </acceptance_criteria>
  <done>The player can see the permanent consequences of the run and immediately start another expedition from the entrance.</done>
</task>

</tasks>

<verification>
- [ ] npm run build-nolog succeeds
- [ ] BattleScene accepts run-specific launch data and returns a structured completion event
- [ ] Defeat, extract, and boss-clear all result in an entrance summary with permanent stash mutation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 01 delivers a full keep-or-lose expedition loop from entrance to terminal summary
</success_criteria>

<output>
After completion, create `.planning/phases/01-first-playable-expedition/01-03-SUMMARY.md`
</output>
