---
phase: "01-first-playable-expedition"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "public/data/mijing/prototype-map.json"
  - "public/data/mijing/prototype-events.json"
  - "public/data/mijing/prototype-shop.json"
  - "public/data/world/initial-state.json"
  - "src/game/types/expedition.ts"
  - "src/game/state/ExpeditionState.ts"
  - "src/game/services/RunPersistence.ts"
autonomous: true
requirements: ["RUN-02", "MAP-03"]
user_setup: []
must_haves:
  truths:
    - "A persistent stash exists before any expedition run starts"
    - "Entering a run creates a saved snapshot of carried deck, items, and spirit stones"
    - "Prototype expedition data defines battle, event, shop, extract, and boss nodes"
  artifacts:
    - path: "src/game/types/expedition.ts"
      provides: "Typed expedition domain models"
      exports: ["ExpeditionNodeType", "PersistentStash", "RunSnapshot", "BattleLaunchPayload", "RunResolutionSummary"]
    - path: "src/game/state/ExpeditionState.ts"
      provides: "Runtime expedition state container"
      exports: ["ExpeditionState"]
    - path: "src/game/services/RunPersistence.ts"
      provides: "Local persistence for stash and active run"
      exports: ["loadPersistentStash", "savePersistentStash", "loadActiveRun", "saveActiveRun", "clearActiveRun"]
    - path: "public/data/mijing/prototype-map.json"
      provides: "Phase 01 fixed-topology expedition map"
      contains: "\"boss\""
  key_links:
    - from: "src/game/state/ExpeditionState.ts"
      to: "src/game/services/RunPersistence.ts"
      via: "stash and run load-save calls"
      pattern: "loadPersistentStash|saveActiveRun|clearActiveRun"
    - from: "src/game/state/ExpeditionState.ts"
      to: "src/game/types/expedition.ts"
      via: "shared run and result contracts"
      pattern: "RunSnapshot|PersistentStash|RunResolutionSummary"
    - from: "src/game/state/ExpeditionState.ts"
      to: "public/data/mijing/prototype-map.json"
      via: "prototype expedition bootstrap"
      pattern: "prototype-map|currentNodeId|nodeStates"
---

<objective>
Establish the expedition domain, persistence, and prototype content backbone for Phase 01.

Purpose: Give later plans a stable stash / run contract so exploration and battle can share one source of truth.
Output: Expedition types, local persistence helpers, active run state container, and fixed-topology prototype content JSON.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-playable-expedition/01-CONTEXT.md
@README.md
@public/data/docs/秘境.md
@public/data/docs/battle-rules.md
@public/data/world/initial-state.json
@public/data/decks/starter-deck.json
@public/data/types/cards/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define expedition contracts and prototype content files</name>
  <files>src/game/types/expedition.ts, public/data/mijing/prototype-map.json, public/data/mijing/prototype-events.json, public/data/mijing/prototype-shop.json</files>
  <read_first>public/data/docs/秘境.md, public/data/docs/battle-rules.md, public/data/world/initial-state.json, public/data/decks/starter-deck.json, public/data/types/cards/index.ts</read_first>
  <action>Create `src/game/types/expedition.ts` with concrete contracts for `ExpeditionNodeType = 'entrance' | 'battle' | 'event' | 'shop' | 'extract' | 'boss'`, `PersistentStash`, `RunSnapshot`, `RunNodeState`, `BattleLaunchPayload`, and `RunResolutionSummary`. Add `public/data/mijing/prototype-map.json` containing one fixed layered map with explicit node ids, layer numbers, outgoing edges, and payload refs. The map must include exactly one entrance node and at least one node of each required Phase 01 type: battle, event, shop, extract, and boss. Add `public/data/mijing/prototype-events.json` and `public/data/mijing/prototype-shop.json` with node-keyed content pools; event rewards may grant cards, items, or `spiritStones`, and every shop offer must spend a numeric `spiritStones` cost.</action>
  <verify>rg -n '"type": "(entrance|battle|event|shop|extract|boss)"' public/data/mijing/prototype-map.json && rg -n 'ExpeditionNodeType|PersistentStash|RunSnapshot|BattleLaunchPayload|RunResolutionSummary|spiritStones' src/game/types/expedition.ts && rg -n 'nodeId|rewards' public/data/mijing/prototype-events.json && rg -n 'cost|spiritStones' public/data/mijing/prototype-shop.json</verify>
  <acceptance_criteria>
    - public/data/mijing/prototype-map.json contains `"type": "entrance"`, `"type": "battle"`, `"type": "event"`, `"type": "shop"`, `"type": "extract"`, and `"type": "boss"`
    - public/data/mijing/prototype-events.json contains `"nodeId"` and `"rewards"`
    - public/data/mijing/prototype-shop.json contains `"cost"` and `"spiritStones"`
    - src/game/types/expedition.ts contains `export type ExpeditionNodeType` and `export interface RunSnapshot`
  </acceptance_criteria>
  <done>Expedition contracts and prototype content files exist for every required node type.</done>
</task>

<task type="auto">
  <name>Task 2: Implement persistent stash and active run storage</name>
  <files>src/game/state/ExpeditionState.ts, src/game/services/RunPersistence.ts, public/data/world/initial-state.json</files>
  <read_first>src/game/scenes/battle/BattleScene.ts, public/data/world/initial-state.json, public/data/decks/starter-deck.json, src/game/types/expedition.ts</read_first>
  <action>Create `src/game/services/RunPersistence.ts` with `STASH_STORAGE_KEY = 'cardgame.persistent-stash.v1'` and `ACTIVE_RUN_STORAGE_KEY = 'cardgame.active-run.v1'`, plus `loadPersistentStash`, `savePersistentStash`, `loadActiveRun`, `saveActiveRun`, and `clearActiveRun`. Extend `public/data/world/initial-state.json` with a `stash` seed that references the starter deck, a small starter item set, and a numeric `spiritStones` balance. Create `src/game/state/ExpeditionState.ts` that owns `persistentStash`, `activeRun`, `createRunSnapshot`, `applyNodeRewardPreview`, and `resetToEntranceState`, and keeps carried deck / items / currency separate from the home stash totals so defeat or extract can mutate them deterministically later.</action>
  <verify>rg -n 'cardgame\.persistent-stash\.v1|cardgame\.active-run\.v1|loadPersistentStash|saveActiveRun|clearActiveRun' src/game/services/RunPersistence.ts && rg -n 'createRunSnapshot|applyNodeRewardPreview|resetToEntranceState|persistentStash|activeRun' src/game/state/ExpeditionState.ts && rg -n '"stash"|"spiritStones"' public/data/world/initial-state.json</verify>
  <acceptance_criteria>
    - src/game/services/RunPersistence.ts contains `cardgame.persistent-stash.v1` and `cardgame.active-run.v1`
    - src/game/services/RunPersistence.ts contains `export function loadPersistentStash` and `export function clearActiveRun`
    - src/game/state/ExpeditionState.ts contains `createRunSnapshot(` and `resetToEntranceState(`
    - public/data/world/initial-state.json contains `"stash"` and `"spiritStones"`
  </acceptance_criteria>
  <done>Local persistence can load or seed a stash and save an active run snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the foundation against the current build flow</name>
  <files>src/game/types/expedition.ts, src/game/state/ExpeditionState.ts, src/game/services/RunPersistence.ts, public/data/mijing/prototype-map.json, public/data/mijing/prototype-events.json, public/data/mijing/prototype-shop.json, public/data/world/initial-state.json</files>
  <read_first>package.json, tsconfig.json, vite/config.dev.mjs</read_first>
  <action>Wire the new expedition files so they compile under the existing Vite + TypeScript build without changing the boot scene yet. If the new JSON files need helpers or shared utility types, add only the minimal import surface required for the current build. Do not start the expedition scene in this plan; the output here is a stable domain and persistence layer that later plans can consume.</action>
  <verify>npm run build-nolog</verify>
  <acceptance_criteria>
    - `npm run build-nolog` exits 0
    - No file modified in this plan contains the string `TODO`
    - src/game/types/expedition.ts, src/game/state/ExpeditionState.ts, and src/game/services/RunPersistence.ts all exist and are referenced by the build graph
  </acceptance_criteria>
  <done>The expedition backbone compiles and is ready for scene integration.</done>
</task>

</tasks>

<verification>
- [ ] npm run build-nolog succeeds
- [ ] Prototype expedition JSON files exist for every Phase 01 node type
- [ ] Persistent stash and active run helpers expose the exact functions named in this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Expedition domain and persistence are ready for scene work without changing combat behavior yet
</success_criteria>

<output>
After completion, create `.planning/phases/01-first-playable-expedition/01-01-SUMMARY.md`
</output>
